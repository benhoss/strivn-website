---
import { landingContent, type Locale } from '../data/landingContent';
import type { FeatureSlug } from '../data/featuresContent';

interface Props {
  locale: Locale;
  slug: FeatureSlug;
}

const { locale, slug } = Astro.props;
const c = landingContent[locale];

/** Whole visual is one illustration for assistive tech — sample data inside is decorative. */
const ariaLabelsByLocale: Record<Locale, Record<FeatureSlug, string>> = {
  fr: {
    communication: 'Illustration : une convocation avec les réponses des joueurs et le décompte des présences en direct.',
    medical: 'Illustration : un dossier blessure avec retour au jeu par étapes et notes du staff.',
    'training-load': 'Illustration : un tableau de charge hebdomadaire avec RPE et signaux ACWR.',
    sessions: 'Illustration : un plan de séance en quatre ateliers minutés.',
    live: 'Illustration : un match en direct avec le score, les buts minute par minute et le nombre de personnes qui suivent via le lien public.',
    scouting: 'Illustration : la fiche d’un adversaire avec ses joueurs, leurs tags d’observation et le statut de rapport partagé.',
    reports: 'Illustration : un rapport d’équipe composé de blocs présences, charge et médical, avec le statut PDF prêt.',
  },
  en: {
    communication: 'Illustration: a call-up message with player replies and a live attendance tally.',
    medical: 'Illustration: an injury record with staged return-to-play and staff notes.',
    'training-load': 'Illustration: a weekly load table with RPE values and ACWR risk flags.',
    sessions: 'Illustration: a session plan with four timed drill phases.',
    live: 'Illustration: a live match panel with the score, goals minute by minute and the number of people following through the public link.',
    scouting: 'Illustration: an opponent file with roster rows, observation tags and a shared-report status.',
    reports: 'Illustration: a team report composed of attendance, load and medical blocks, with a PDF-ready status.',
  },
  nl: {
    communication: 'Illustratie: een oproeping met de antwoorden van de spelers en een live aanwezigheidsteller.',
    medical: 'Illustratie: een blessuredossier met gefaseerde return-to-play en notities van de staf.',
    'training-load': 'Illustratie: een weektabel van de belasting met RPE-waarden en ACWR-signalen.',
    sessions: 'Illustratie: een trainingsplan met vier getimede oefenblokken.',
    live: 'Illustratie: een livewedstrijd met de score, de doelpunten minuut per minuut en het aantal mensen dat via de publieke link meevolgt.',
    scouting: 'Illustratie: de fiche van een tegenstander met zijn spelers, observatietags en de status gedeeld rapport.',
    reports: 'Illustratie: een teamrapport opgebouwd uit de blokken aanwezigheid, belasting en medisch, met de status PDF klaar.',
  },
  de: {
    communication: 'Illustration: eine Aufstellung mit den Antworten der Spieler und einer Live-Anwesenheitszählung.',
    medical: 'Illustration: eine Verletzungsakte mit stufenweisem Return-to-Play und Notizen des Staffs.',
    'training-load': 'Illustration: eine Wochentabelle der Belastung mit RPE-Werten und ACWR-Signalen.',
    sessions: 'Illustration: ein Trainingsplan mit vier getimten Übungsblöcken.',
    live: 'Illustration: ein Live-Spiel mit Spielstand, Toren Minute für Minute und der Zahl der Personen, die über den öffentlichen Link mitverfolgen.',
    scouting: 'Illustration: die Akte eines Gegners mit seinen Spielern, Beobachtungs-Tags und dem Status geteilter Bericht.',
    reports: 'Illustration: ein Team-Bericht aus den Blöcken Anwesenheit, Belastung und Medizinisches, mit dem Status PDF bereit.',
  },
};
const ariaLabels = ariaLabelsByLocale[locale];

type Chip = { tone: 'ok' | 'watch' | 'risk' | 'info'; text: string };
const chipsByLocale: Record<Locale, Record<FeatureSlug, Chip>> = {
  fr: {
    communication: { tone: 'ok', text: 'L. Moreau — Présent · 18h09' },
    medical: { tone: 'watch', text: 'Réathlétisation · J+9' },
    'training-load': { tone: 'risk', text: 'ACWR 1.32 — alléger vendredi' },
    sessions: { tone: 'info', text: 'Ajustée à la charge du groupe' },
    live: { tone: 'ok', text: '14 suivent via le lien public' },
    scouting: { tone: 'info', text: 'Note de l’analyste ajoutée' },
    reports: { tone: 'ok', text: 'PDF prêt à partager' },
  },
  en: {
    communication: { tone: 'ok', text: 'L. Moreau — In · 6:09pm' },
    medical: { tone: 'watch', text: 'Reconditioning · day 9' },
    'training-load': { tone: 'risk', text: 'ACWR 1.32 — lighten Friday' },
    sessions: { tone: 'info', text: 'Tuned to the squad’s load' },
    live: { tone: 'ok', text: '14 watching via the public link' },
    scouting: { tone: 'info', text: 'Analyst note added' },
    reports: { tone: 'ok', text: 'PDF ready to share' },
  },
  nl: {
    communication: { tone: 'ok', text: 'L. Moreau — Aanwezig · 18u09' },
    medical: { tone: 'watch', text: 'Revalidatie · dag 9' },
    'training-load': { tone: 'risk', text: 'ACWR 1.32 — vrijdag verlichten' },
    sessions: { tone: 'info', text: 'Afgestemd op de belasting van de groep' },
    live: { tone: 'ok', text: '14 volgen via de publieke link' },
    scouting: { tone: 'info', text: 'Notitie van de analist toegevoegd' },
    reports: { tone: 'ok', text: 'PDF klaar om te delen' },
  },
  de: {
    communication: { tone: 'ok', text: 'L. Moreau — Dabei · 18:09' },
    medical: { tone: 'watch', text: 'Reha · Tag 9' },
    'training-load': { tone: 'risk', text: 'ACWR 1.32 — Freitag reduzieren' },
    sessions: { tone: 'info', text: 'Auf die Belastung der Gruppe abgestimmt' },
    live: { tone: 'ok', text: '14 folgen über den öffentlichen Link' },
    scouting: { tone: 'info', text: 'Notiz des Analysten hinzugefügt' },
    reports: { tone: 'ok', text: 'PDF bereit zum Teilen' },
  },
};
const chip = chipsByLocale[locale][slug];

/** Sample match panel for the `live` slug — decorative data, no landingContent entry exists for it. */
type LiveSample = {
  badge: string;
  minute: string;
  home: string;
  away: string;
  score: string;
  events: Array<{ min: string; text: string }>;
};
const liveByLocale: Record<Locale, LiveSample> = {
  fr: {
    badge: 'EN DIRECT',
    minute: '64’',
    home: 'AC Verel',
    away: 'FC Boisval',
    score: '2 – 1',
    events: [
      { min: '12’', text: 'But · N. Carvalho' },
      { min: '38’', text: 'But · FC Boisval' },
      { min: '57’', text: 'But · Y. Benali' },
    ],
  },
  en: {
    badge: 'LIVE',
    minute: '64’',
    home: 'AC Verel',
    away: 'FC Boisval',
    score: '2 – 1',
    events: [
      { min: '12’', text: 'Goal · N. Carvalho' },
      { min: '38’', text: 'Goal · FC Boisval' },
      { min: '57’', text: 'Goal · Y. Benali' },
    ],
  },
  nl: {
    badge: 'LIVE',
    minute: '64’',
    home: 'AC Verel',
    away: 'FC Boisval',
    score: '2 – 1',
    events: [
      { min: '12’', text: 'Doelpunt · N. Carvalho' },
      { min: '38’', text: 'Doelpunt · FC Boisval' },
      { min: '57’', text: 'Doelpunt · Y. Benali' },
    ],
  },
  de: {
    badge: 'LIVE',
    minute: '64’',
    home: 'AC Verel',
    away: 'FC Boisval',
    score: '2 – 1',
    events: [
      { min: '12’', text: 'Tor · N. Carvalho' },
      { min: '38’', text: 'Tor · FC Boisval' },
      { min: '57’', text: 'Tor · Y. Benali' },
    ],
  },
};

/** Sample opponent-scouting panel for the `scouting` slug — decorative data, no landingContent entry exists for it. */
type ScoutingSample = {
  label: string;
  opponent: string;
  shared: string;
  players: Array<{ num: string; name: string; tags: string[] }>;
};
const scoutingByLocale: Record<Locale, ScoutingSample> = {
  fr: {
    label: 'Prochain adversaire',
    opponent: 'FC Boisval',
    shared: 'Rapport partagé',
    players: [
      { num: '9', name: 'M. Costa', tags: ['Vitesse', 'Pied gauche'] },
      { num: '10', name: 'T. Willems', tags: ['Coups francs', 'Meneur'] },
      { num: '4', name: 'S. Dubois', tags: ['Jeu aérien', 'Relance longue'] },
    ],
  },
  en: {
    label: 'Next opponent',
    opponent: 'FC Boisval',
    shared: 'Report shared',
    players: [
      { num: '9', name: 'M. Costa', tags: ['Pace', 'Left foot'] },
      { num: '10', name: 'T. Willems', tags: ['Free kicks', 'Playmaker'] },
      { num: '4', name: 'S. Dubois', tags: ['Aerial threat', 'Long balls'] },
    ],
  },
  nl: {
    label: 'Volgende tegenstander',
    opponent: 'FC Boisval',
    shared: 'Rapport gedeeld',
    players: [
      { num: '9', name: 'M. Costa', tags: ['Snelheid', 'Linksbenig'] },
      { num: '10', name: 'T. Willems', tags: ['Vrije trappen', 'Spelmaker'] },
      { num: '4', name: 'S. Dubois', tags: ['Kopsterk', 'Lange bal'] },
    ],
  },
  de: {
    label: 'Nächster Gegner',
    opponent: 'FC Boisval',
    shared: 'Bericht geteilt',
    players: [
      { num: '9', name: 'M. Costa', tags: ['Tempo', 'Linker Fuß'] },
      { num: '10', name: 'T. Willems', tags: ['Freistöße', 'Spielmacher'] },
      { num: '4', name: 'S. Dubois', tags: ['Kopfballstark', 'Langer Ball'] },
    ],
  },
};

/** Sample report-builder panel for the `reports` slug — decorative data, no landingContent entry exists for it. */
type ReportsSample = {
  label: string;
  name: string;
  ready: string;
  blocks: Array<{ name: string; detail: string }>;
};
const reportsByLocale: Record<Locale, ReportsSample> = {
  fr: {
    label: 'Rapport d’équipe',
    name: 'Bilan du mois — U17',
    ready: 'PDF prêt',
    blocks: [
      { name: 'Présences', detail: '12 séances' },
      { name: 'Charge', detail: 'ACWR stable' },
      { name: 'Médical', detail: '1 retour au jeu' },
    ],
  },
  en: {
    label: 'Team report',
    name: 'Monthly review — U17',
    ready: 'PDF ready',
    blocks: [
      { name: 'Attendance', detail: '12 sessions' },
      { name: 'Load', detail: 'ACWR steady' },
      { name: 'Medical', detail: '1 return to play' },
    ],
  },
  nl: {
    label: 'Teamrapport',
    name: 'Maandoverzicht — U17',
    ready: 'PDF klaar',
    blocks: [
      { name: 'Aanwezigheid', detail: '12 trainingen' },
      { name: 'Belasting', detail: 'ACWR stabiel' },
      { name: 'Medisch', detail: '1 return-to-play' },
    ],
  },
  de: {
    label: 'Team-Bericht',
    name: 'Monatsbilanz — U17',
    ready: 'PDF bereit',
    blocks: [
      { name: 'Anwesenheit', detail: '12 Einheiten' },
      { name: 'Belastung', detail: 'ACWR stabil' },
      { name: 'Medizinisch', detail: '1 Return-to-Play' },
    ],
  },
};

const com = c.communication.proof;
const med = c.medical.record;
const load = c.load.table;
const board = c.sessions.board;
const live = liveByLocale[locale];
const scout = scoutingByLocale[locale];
const rep = reportsByLocale[locale];
---

<div class="fv" role="img" aria-label={ariaLabels[slug]}>
  <!-- Tactical-ink pitch drawing, faint, behind the panel -->
  <svg class="fv__pitch" viewBox="0 0 560 440" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
    <!-- halfway line + centre circle -->
    <line x1="40" y1="20" x2="40" y2="420" stroke="currentColor" stroke-width="1.5" />
    <circle cx="40" cy="220" r="96" stroke="currentColor" stroke-width="1.5" />
    <circle cx="40" cy="220" r="4" fill="currentColor" />
    <!-- penalty box -->
    <rect x="392" y="96" width="168" height="248" stroke="currentColor" stroke-width="1.5" />
    <rect x="486" y="158" width="74" height="124" stroke="currentColor" stroke-width="1.5" />
    <circle cx="448" cy="220" r="3" fill="currentColor" />
    <path d="M392 164 A 84 84 0 0 0 392 276" stroke="currentColor" stroke-width="1.5" />
    <!-- coached run: dashed path with arrowhead -->
    <path d="M96 332 C 200 300, 250 180, 380 150" stroke="currentColor" stroke-width="1.5" stroke-dasharray="2 7" stroke-linecap="round" />
    <path d="M370 142 L 384 149 L 372 158" stroke="currentColor" stroke-width="1.5" stroke-linejoin="round" />
    <!-- players -->
    <circle cx="96" cy="332" r="6" stroke="currentColor" stroke-width="1.5" />
    <circle cx="284" cy="236" r="6" stroke="currentColor" stroke-width="1.5" />
    <path d="M330 308 l 12 12 m 0 -12 l -12 12" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" />
  </svg>

  <div class="fv__panel">
    {slug === 'communication' && (
      <div class="s-proof">
        <span class="s-label s-proof__label">{com.messageLabel}</span>
        <p class="s-flow__msg">
          {com.message}
          <span class="s-flow__link">{com.link}</span>
        </p>
        <ul class="s-flow__replies">
          {com.replies.map((reply) => (
            <li class="s-flow__reply">
              <span class="s-flow__name">{reply.name}</span>
              <span class={`s-flow__answer ${reply.tone === 'watch' ? 's-flow__answer--watch' : ''}`}>{reply.answer}</span>
            </li>
          ))}
        </ul>
        <p class="s-flow__tally">{com.tally}</p>
      </div>
    )}

    {slug === 'medical' && (
      <div class="s-proof">
        <div class="s-record__head">
          <p>
            <span class="s-record__player">{med.player}</span>
            <span class="s-record__injury">{med.injury}</span>
          </p>
          <span class="s-record__since">{med.since}</span>
        </div>
        <ul class="s-record__stages">
          {med.stages.map((stage) => (
            <li class={`s-record__stage s-record__stage--${stage.state}`}>
              <span class="s-record__stage-bar" aria-hidden="true"></span>
              <span class="s-record__stage-label">{stage.label}</span>
            </li>
          ))}
        </ul>
        <ul class="s-record__notes">
          {med.notes.map((note) => (
            <li class="s-record__note">
              <span class="s-record__note-author">{note.author}</span>
              <span class="s-record__note-role">{note.role}</span>
              <span class="s-record__note-text">{note.text}</span>
            </li>
          ))}
        </ul>
        <p class="s-record__return">
          <span class="s-label">{med.returnLabel}</span>
          <span class="s-record__return-value">{med.returnValue}</span>
        </p>
      </div>
    )}

    {slug === 'training-load' && (
      <div class="s-proof">
        <table class="s-table">
          <caption>
            <span class="s-label">{load.title} · {load.week}</span>
          </caption>
          <thead>
            <tr>
              <th scope="col">{load.colPlayer}</th>
              <th scope="col" data-num>{load.colMon}</th>
              <th scope="col" data-num>{load.colWed}</th>
              <th scope="col" data-num>{load.colFri}</th>
              <th scope="col" data-num>{load.colAcwr}</th>
              <th scope="col">{load.colState}</th>
            </tr>
          </thead>
          <tbody>
            {load.players.map((player) => (
              <tr>
                <td>{player.name}</td>
                <td data-num>{player.mon}</td>
                <td data-num>{player.wed}</td>
                <td data-num>{player.fri}</td>
                <td data-num>{player.acwr}</td>
                <td>
                  <span class={`s-zone s-zone--${player.zone}`}>
                    {player.zone === 'green' ? load.pillOk : player.zone === 'amber' ? load.pillWatch : load.pillRisk}
                  </span>
                </td>
              </tr>
            ))}
          </tbody>
        </table>
        <p class="s-table__tip">
          <strong>{load.tipLabel}</strong>
          {load.tip}
        </p>
      </div>
    )}

    {slug === 'sessions' && (
      <div class="s-proof">
        <div class="s-board__head">
          <p>
            <span class="s-board__title">{board.title}</span>
            <span class="s-board__meta">{board.meta}</span>
          </p>
          <span class="s-board__stamp">{board.stamp}</span>
        </div>
        <ol class="s-board__phases">
          {board.phases.map((phase) => (
            <li class="s-board__phase">
              <p>
                <span class="s-board__phase-label">{phase.label}</span>
                <span class="s-board__phase-detail">{phase.detail}</span>
              </p>
            </li>
          ))}
        </ol>
      </div>
    )}

    {slug === 'live' && (
      <div class="s-proof">
        <div class="fv-live__head">
          <span class="fv-live__badge">{live.badge}</span>
          <span class="fv-live__minute">{live.minute}</span>
        </div>
        <p class="fv-live__score">
          <span class="fv-live__team">{live.home}</span>
          <span class="fv-live__nums">{live.score}</span>
          <span class="fv-live__team fv-live__team--away">{live.away}</span>
        </p>
        <ul class="fv-live__events">
          {live.events.map((event) => (
            <li class="fv-live__event">
              <span class="fv-live__event-min">{event.min}</span>
              <span class="fv-live__event-text">{event.text}</span>
            </li>
          ))}
        </ul>
      </div>
    )}

    {slug === 'scouting' && (
      <div class="s-proof">
        <div class="fv-scout__head">
          <p class="fv-scout__title">
            <span class="s-label">{scout.label}</span>
            <span class="fv-scout__opponent">{scout.opponent}</span>
          </p>
          <span class="fv-scout__shared">{scout.shared}</span>
        </div>
        <ul class="fv-scout__players">
          {scout.players.map((player) => (
            <li class="fv-scout__player">
              <span class="fv-scout__num">{player.num}</span>
              <span class="fv-scout__name">{player.name}</span>
              <span class="fv-scout__tags">
                {player.tags.map((tag) => (
                  <span class="fv-scout__tag">{tag}</span>
                ))}
              </span>
            </li>
          ))}
        </ul>
      </div>
    )}

    {slug === 'reports' && (
      <div class="s-proof">
        <div class="fv-report__head">
          <p class="fv-report__title">
            <span class="s-label">{rep.label}</span>
            <span class="fv-report__name">{rep.name}</span>
          </p>
          <span class="fv-report__ready">{rep.ready}</span>
        </div>
        <ul class="fv-report__blocks">
          {rep.blocks.map((block) => (
            <li class="fv-report__block">
              <span class="fv-report__block-name">{block.name}</span>
              <span class="fv-report__block-detail">{block.detail}</span>
            </li>
          ))}
        </ul>
      </div>
    )}

    <span class={`fv__chip fv__chip--${chip.tone}`}>{chip.text}</span>
  </div>
</div>

<style>
  .fv {
    position: relative;
  }

  .fv__pitch {
    position: absolute;
    inset: -14% -10% auto;
    width: 120%;
    color: var(--electric-500);
    opacity: 0.11;
    pointer-events: none;
  }

  .fv__panel {
    position: relative;
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-chrome-lg);
  }

  .fv__chip {
    position: absolute;
    right: calc(-1 * var(--space-md));
    bottom: calc(-1 * var(--space-md));
    display: inline-flex;
    align-items: center;
    gap: var(--space-xs);
    max-width: calc(100% - var(--space-md));
    padding: var(--space-xs) var(--space-sm);
    background: var(--nav-backdrop);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border: 1px solid var(--rule-strong);
    border-radius: var(--radius-pill);
    box-shadow: var(--shadow-chrome);
    font-family: var(--font-mono);
    font-size: var(--text-label);
    letter-spacing: 0.03em;
    color: var(--ink-2);
    white-space: nowrap;
  }

  .fv__chip::before {
    content: '';
    flex: none;
    width: 6px;
    height: 6px;
    border-radius: var(--radius-pill);
  }

  .fv__chip--ok::before {
    background: var(--performance);
  }

  .fv__chip--watch::before {
    background: var(--warning);
  }

  .fv__chip--risk::before {
    background: var(--risk);
  }

  .fv__chip--info::before {
    background: var(--electric-500);
  }

  .fv-live__head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: var(--space-md);
  }

  .fv-live__badge {
    display: inline-flex;
    align-items: center;
    gap: var(--space-xs);
    font-family: var(--font-mono);
    font-size: var(--text-label);
    letter-spacing: 0.08em;
    color: var(--ink-2);
  }

  .fv-live__badge::before {
    content: '';
    flex: none;
    width: 6px;
    height: 6px;
    border-radius: var(--radius-pill);
    background: var(--risk);
  }

  .fv-live__minute {
    font-family: var(--font-mono);
    font-size: var(--text-label);
    color: var(--ink-3);
  }

  .fv-live__score {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: var(--space-sm);
    margin: 0 0 var(--space-md);
  }

  .fv-live__team {
    min-width: 0;
    font-weight: 600;
  }

  .fv-live__team--away {
    text-align: right;
  }

  .fv-live__nums {
    flex: none;
    font-family: var(--font-mono);
    font-size: 1.4rem;
    letter-spacing: 0.04em;
  }

  .fv-live__events {
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .fv-live__event {
    display: flex;
    gap: var(--space-sm);
    padding-block: var(--space-xs);
    border-top: 1px solid var(--rule);
    font-size: var(--text-sm);
  }

  .fv-live__event-min {
    flex: none;
    font-family: var(--font-mono);
    color: var(--ink-3);
  }

  .fv-live__event-text {
    color: var(--ink-2);
  }

  .fv-scout__head {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: var(--space-sm);
    margin-bottom: var(--space-md);
  }

  .fv-scout__title {
    display: flex;
    flex-direction: column;
    gap: var(--space-xs);
    margin: 0;
    min-width: 0;
  }

  .fv-scout__opponent {
    font-weight: 600;
  }

  .fv-scout__shared {
    display: inline-flex;
    align-items: center;
    gap: var(--space-xs);
    flex: none;
    font-family: var(--font-mono);
    font-size: var(--text-label);
    letter-spacing: 0.03em;
    color: var(--ink-2);
  }

  .fv-scout__shared::before {
    content: '';
    flex: none;
    width: 6px;
    height: 6px;
    border-radius: var(--radius-pill);
    background: var(--performance);
  }

  .fv-scout__players {
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .fv-scout__player {
    display: flex;
    align-items: center;
    gap: var(--space-sm);
    padding-block: var(--space-xs);
    border-top: 1px solid var(--rule);
    font-size: var(--text-sm);
  }

  .fv-scout__num {
    flex: none;
    min-width: 1.2em;
    font-family: var(--font-mono);
    color: var(--ink-3);
  }

  .fv-scout__name {
    min-width: 0;
    font-weight: 600;
    color: var(--ink-2);
  }

  .fv-scout__tags {
    display: flex;
    flex-wrap: wrap;
    justify-content: flex-end;
    gap: var(--space-xs);
    margin-left: auto;
  }

  .fv-scout__tag {
    padding: 1px var(--space-xs);
    border: 1px solid var(--rule-strong);
    border-radius: var(--radius-pill);
    font-family: var(--font-mono);
    font-size: var(--text-label);
    color: var(--ink-3);
    white-space: nowrap;
  }

  .fv-report__head {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: var(--space-sm);
    margin-bottom: var(--space-md);
  }

  .fv-report__title {
    display: flex;
    flex-direction: column;
    gap: var(--space-xs);
    margin: 0;
    min-width: 0;
  }

  .fv-report__name {
    font-weight: 600;
  }

  .fv-report__ready {
    display: inline-flex;
    align-items: center;
    gap: var(--space-xs);
    flex: none;
    font-family: var(--font-mono);
    font-size: var(--text-label);
    letter-spacing: 0.03em;
    color: var(--ink-2);
  }

  .fv-report__ready::before {
    content: '';
    flex: none;
    width: 6px;
    height: 6px;
    border-radius: var(--radius-pill);
    background: var(--performance);
  }

  .fv-report__blocks {
    display: grid;
    gap: var(--space-xs);
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .fv-report__block {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-sm);
    padding: var(--space-xs) var(--space-sm);
    border: 1px solid var(--rule);
    border-radius: var(--radius-sm);
    font-size: var(--text-sm);
  }

  .fv-report__block-name {
    min-width: 0;
    font-weight: 600;
    color: var(--ink-2);
  }

  .fv-report__block-detail {
    flex: none;
    font-family: var(--font-mono);
    font-size: var(--text-label);
    color: var(--ink-3);
  }

  @media (max-width: 40rem) {
    .fv__chip {
      right: var(--space-xs);
      white-space: normal;
    }
  }
</style>
