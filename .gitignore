node_modules
dist
.astro
.DS_Store

# Generated local agent/tool caches
.agents/
.claude/
.cursor/
.gemini/
.impeccable/
.kiro/
.opencode/
.qoder/

# Debug screenshots — not part of the site
big*.png
band-*.png
ch-03-*.png
channels-zoom*.png
final-*.png
load-*.png
marked.html
measure.html
scan-*.png

# gstack browse daemon logs
.gstack/

# Secrets — never commit
nano.json
credentials.json
**/credentials.json
*.credentials.json

# Marketing video — rendered outputs and binary media (keep code + markdown only)
launch-video/renders/
launch-video/assets/
launch-video/**/*.mp4
launch-video/**/*.wav
launch-video/**/*.aiff
launch-video/**/*.webm

# Secrets — never commit (ElevenLabs key, etc.)
.env
